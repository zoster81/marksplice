# Marksplice Architecture

Status: source of truth for durable architecture decisions.

## Mission

Marksplice is a Pure-Go library for creating and source-preserving structural manipulation of one Markdown profile: CommonMark 0.31.2 as the normative base grammar plus the published GFM extensions/corrections layered on top.

Marksplice has two deliberately separate core paths:

```text
                       caller intent
                            |
                 +----------+----------+
                 |                     |
          existing GFM bytes      new document
                 |                     |
               Parse            DocumentBuilder
                 |                     |
        immutable snapshot       canonical writer
                 |                     |
       semantic + source map      parser/model proof
                 |                     |
        structural operation            |
                 |                     |
        minimal byte patches            |
                 +----------+----------+
                            |
                         GFM bytes
```

Existing documents are immutable source snapshots. Ordinary edits use minimal source-bound patches and never authorize normalization of untouched source. New documents may use deterministic canonical GFM because there is no author formatting to preserve. New-document writing must never be substituted for the existing-document edit path.

M0 is the green repository-bootstrap baseline and M1–M121 are complete locally. The approved post-M115 sequence is maintained in `docs/roadmap.md`: the pre-M116 `v0.5.0-beta.1` performance campaign optimized the Native/parser-document pipeline before feature growth resumed; M122 is the next engineering boundary after exact M121 remote closure, M122–M124 lead to the v1.0 stability gate, and M125–M126 are the deferred v1.5 PDF line. Detailed completed-milestone chronology, feature contracts, and verification evidence live in `docs/milestones/`; this document records only architecture that remains durable across those milestones.

## Package boundaries

- `internal/parser` owns the complete parser-independent backend contract consumed by Marksplice plus the separate optional internal semantic-event contract used only by on-demand rendering projections.
- `internal/parser/native` is the production `parser.Backend` and owns physical-line/indent views, Marksplice-owned CommonMark/GFM block/inline grammar, full GFM reference-label normalization, parser-independent observations, footnote/math overlays, and construction-proof primitives. M115 removed the former Goldmark adapter and differential harness.
- `internal/parser/native/testdata` retains Marksplice-owned parser-neutral CommonMark/GFM observation fixtures tied by example identity and Markdown hashes to the approved external specification snapshots; it contains no upstream specification corpus bytes.
- `internal/testutil/commonmarkspec` owns the test-only approved CommonMark 0.31.2 snapshot hash/example extraction. `internal/testutil/gfmspec` owns the approved published GFM snapshot and extension-section classification. Both external snapshots remain untracked validation input.
- `internal/source` owns parser-independent byte ranges, lexical source proof, source fingerprints, validated patches, and patch application.
- `internal/splice` combines parser observations with source mappings, builds immutable snapshot indexes/relationships, prepares structural mutations, and bridges immutable document source to on-demand semantic rendering without retaining renderer state.
- `internal/renderhtml` owns deterministic streaming HTML-fragment emission plus the small standalone document wrapper from `parser.SemanticEvent` values, with explicit raw-HTML, unsafe-URL, tag-filter, and reviewed metadata policy. It does not parse Markdown or perform filesystem/network/command I/O.
- `internal/publictest` owns black-box tests that import the root module API exactly as an external Go consumer would.
- root package `marksplice` owns reviewed public types and operations. It may wrap internal implementation values but must not expose parser-internal or `internal/*` types.
- top-level package `workspacefs` is the opt-in read-only filesystem adapter. It accepts caller-provided `fs.FS` authority, parses through root `marksplice.Parse`, and delegates graph/validation semantics back to the existing root APIs rather than owning a second document graph or parser.

The public package deliberately remains at the Go module root because the canonical import path is `github.com/zoster81/marksplice`. Moving that package under a top-level `src/` directory would change the natural import path to `github.com/zoster81/marksplice/src` or require a forwarding facade whose only purpose is repository cosmetics. Root Go filenames are therefore grouped by responsibility instead: `api*.go` owns parsed-document/read/edit APIs and `builder*.go` owns new-document construction plus its package-private proof/writer helpers. Longer-form documentation stays under `docs/`, while external-style public API tests stay out of the root under `internal/publictest/`.

The approved post-M115 architecture adds separate layers without weakening those boundaries. M116 introduces the filesystem-facing `workspacefs` package: `Scan` performs deterministic `.md`/`.markdown` discovery beneath one explicit `fs.FS` root, `Follow` performs cycle-safe relationship-driven loading from explicit entries, and both fail closed under document/byte/depth/relationship budgets. Loaded keys are slash-relative to the supplied workspace root. M117 freezes the shared relationship resolver used by `Follow`, graph construction, and workspace validation: literal relative dot segments normalize against the source document only while the result remains in the supplied `fs.FS` namespace; path components are percent-decoded exactly once; query text is excluded from filesystem lookup; target fragments remain intact for existing fragment resolution. Absolute paths, URI schemes, protocol-relative forms, raw backslashes, empty path segments, malformed encoding, encoded traversal or separators, directory targets, and extensionless targets are not filesystem document relationships. Case sensitivity, symlink behavior, and other host semantics remain properties of the caller-provided `fs.FS`; Marksplice does not treat `fs.FS` as a universal security sandbox. M118 adds a separate internal `SemanticBackend`/event vocabulary and a Native `WalkSemantic` implementation. M119 completes that stream by capturing recursive block ownership at the Native parse point, applying the existing source-layer front-matter envelope before Markdown body parsing, sharing exact GitHub alert marker policy with the public alert surface, and replacing footnote/math source regions in place rather than appending supplemental semantics. Inline emission reuses Native ownership, task-prefix exclusion, reference resolution, autolink classification, and code-span delimiter facts; renderers therefore do not reparse Markdown. All capture, terminal, and child-index state is operation-local and discarded when the walk returns. Ordinary `parser.Backend.ParseDocument` invokes the same block machinery with semantic capture disabled, so every parsed `Document` still does not retain a rendering AST or rendering index. M120 adds `internal/renderhtml` and the public `RenderHTML`/`HTML` entry points over that same on-demand walk: the renderer consumes semantic events, never reparses Markdown delimiters, and retains only operation-local stack state plus bounded local buffers needed for image alt text and footnote output. M121 adds `RenderHTMLDocument`/`HTMLDocument` as a deterministic wrapper around that exact fragment path. Its metadata projection scans only the already-recognized leading front-matter region, maps a fixed four-key scalar set, and never reparses YAML/TOML or retains template/metadata state. PDF remains deferred to the v1.5 line and is expected to consume HTML through a separately reviewed backend boundary rather than adding browser/font/network/command authority to the parser/editing core.

Keep orchestration separate from syntax-specific proof. Shared plumbing may centralize target lookup, patch transforms, candidate assembly, and candidate parsing, while lexical/source mappers and semantic validators remain feature-specific when their safety invariants differ. Reuse focused lexical primitives rather than duplicating scanners across families.

## Public API stability, concurrency, and boundedness

M109 treats the v0 beta stabilization window as the point to remove accidental public ambiguity without widening source authority for symmetry. The only reviewed source-level API reshaping replaces `WorkspaceDiagnostic.UnresolvedReference`'s adjacent semantic/presence booleans with one immutable `UnresolvedReference` value plus the conventional `(value, found)` result. Existing mutation, query, graph, workspace, knowledge, fenced-block, footnote, math, front-matter, and reference asymmetries remain intentional when their source-ownership or semantic proof differs.

Successfully built immutable `Document`, `DocumentGraph`, `KnowledgeIndex`, `WorkspaceReport`, `workspacefs.Workspace`, and prepared `ChangeSet` values are safe for concurrent reads, query/mutation planning, and `ChangeSet.Apply`. Public variable-length results are defensive/caller-owned copies unless explicitly documented otherwise. This guarantee does not permit callers to mutate argument byte slices concurrently with an operation. `DocumentBuilder` remains mutable and requires caller synchronization for concurrent use.

Graph/workspace resolver callbacks are invoked synchronously and serially within one build/validation call and are never retained. Core operations perform no implicit filesystem/network I/O. `workspacefs` adds only explicit read authority through the caller's `fs.FS`; it performs no writes/network/commands and enforces finite load/traversal budgets before returning a workspace. Structural queries retain explicit positive result limits; root graph/workspace/knowledge operations remain bounded by their finite caller-supplied document sets and do not add hidden global result caps. The current parser/proof contract does not expose a `context.Context` parameter; cancellation or additional budget checkpoints require an explicit reviewed API/implementation contract that can honor them deterministically rather than a facade that cannot interrupt parsing reliably.

Public sentinel errors classify actionable failure families and callers should use `errors.Is`; diagnostic message wording is not a compatibility contract. `Kind` is the closed Marksplice core structural namespace. M110 extension identities use separate namespaced/typed identities and cannot register, replace, or consume core `Kind` ordinals.

## Third-party extension overlay

M110 defines the public third-party boundary as a read-only overlay above the ordinary core parse rather than a parser plugin point. `Parse` remains the baseline GFM API. `ParseWithOptions` validates the complete extension configuration first, performs the ordinary immutable Marksplice parse, and only then invokes explicitly registered `ExtensionRecognizer` callbacks synchronously and serially in registration order. Zero options are behaviorally equivalent to `Parse`.

Each recognizer receives one immutable `ExtensionSource` string copy of the exact parsed source and returns only extension-local kinds, non-empty snapshot-local `Range` values, and scalar attributes. Marksplice validates namespace/kind/attribute tokens, UTF-8/NUL constraints, ranges, duplicate metadata names, and caller-provided total node/metadata-byte budgets before retaining anything. Results are defensively copied; extension nodes and their attributes are immutable/caller-owned on read and participate in the existing concurrent-read guarantee. Recognizer errors or recovered panics are wrapped with `ErrInvalidExtension`, and the complete operation fails without publishing partial extension state.

Extension observations are intentionally separate from core `Node`, `NodeID`, and `Kind`; they cannot suppress, replace, or reclassify baseline GFM structure. M110 adds no generic source mutation, `ChangeSet`, `DocumentBuilder`, graph, resolver, parser-AST, filesystem, network, command, or rendering authority. Overlapping observations are permitted because they are independent read-only claims. Recognizers are ordinary statically linked caller code: Marksplice validates/bounds only retained observations and cannot sandbox or preempt the recognizer's own execution or external authority.

The overlay has no parser-specific type or callback contract and requires no access to `internal/parser`, `internal/source`, or `internal/splice`. This is deliberate: M111–M115 preserved the public M110 boundary while replacing the core parser implementation beneath it.

## Source model and public promotion

Source is represented as bytes. Ranges are half-open byte offsets `[start,end)` into one immutable snapshot. Byte offsets, not rune indexes, define mutation boundaries.

A parsed document may retain compact derived indexes and adjacency needed for efficient navigation, but the ordered node collection remains the source of structural iteration order. Snapshot `NodeID` values are deterministic and comparable only within the snapshot whose source facts produced them; they are not durable identities across arbitrary revisions. Duplicate derived IDs must fail closed rather than silently select one node.

Semantic recognition alone is not enough for public promotion. An actionable public kind requires reviewed caller-facing semantics plus exact source ownership for the operation it exposes. Unsupported or ambiguous semantic shapes remain internal/non-editable instead of receiving guessed ranges or partial mutation guarantees.

Typed public ranges are operation-oriented. Marksplice intentionally does not expose one generic "full node span" contract: a paragraph range, list-item content range, list subtree range, table-cell range, blockquote line range, or link destination range means exactly the bytes documented by that typed capability.

`Document.SourceRange` validates a snapshot-local public range and returns caller-owned bytes. Public accessors returning variable-length relationships or metadata return defensive copies so callers cannot mutate internal snapshot state.

## Structural query model

M97 adds bounded selectors over the same immutable snapshot model; it does not create a second AST, mutable query document, or persistent selector index.

`Document.QueryNodes` scans the existing source-ordered internal node array and returns only nodes already eligible for ordinary public promotion. Callers may filter by reviewed public `Kind`, optional complete containment in one snapshot-local byte range, and a mandatory positive result limit. `Document.QuerySections` reuses the existing compact section array and `Section` representation, with optional level and complete-subtree containment filters plus the same explicit positive result bound.

`NodeMatch.Range()` is intentionally not a generic full-node source span. It reuses the exact operation-oriented range already defined by that node kind's typed `Range()` accessor and exists only for selection/bounded reading. Mutation authority remains in the typed mutation operations and their source/candidate proof.

Queries preserve authoritative source/structural order and stop as soon as the caller's limit is reached. Kind and section-level filters are represented by fixed-size boolean arrays and reject oversized duplicate input vectors. Query-specific snapshot projection lives in `internal/splice/query.go`: `NodeSelectionAt` returns one shared scalar node summary plus the already-reviewed selection range without cloning the full internal node or its variable-length table/blockquote metadata, while `SourceLen` validates `Within` without copying source bytes. Ordinary `NodeSummaryAt` enumeration remains query-independent and reuses the same scalar summarization helper.

No query state is retained in `Document`. Navigation and relationship intelligence do not extend M97 into a string/regex/graph query language.

## Navigation model

M98 derives heading anchors, fragment targets, and TOCs from the same immutable structural snapshot rather than creating a navigation AST or persistent index. The parser adapter contributes one plain semantic `HeadingText` string for promoted document headings; parser-specific AST types remain confined to the adapter, and source ownership/ranges remain independent from that semantic text.

Heading anchors are recomputed in source order with an ephemeral duplicate-reservation map. Fragment resolution URI-decodes one local fragment and scans heading-derived anchors plus the already-promoted source-proven HTML anchor subset; zero or multiple matches fail closed. Explicit HTML anchors do not participate in heading duplicate numbering or generated TOC entries. Broader link/reference relationship enumeration remains M99.

TOC generation reuses the existing `Section` hierarchy and parent links. A generated standalone TOC is deterministic LF Markdown. Existing-source synchronization is narrower: the caller designates one section, its exact `BodyRange` must be empty/blank or match the conservative managed local-fragment-list grammar, its established line-ending form and leading/trailing blank trivia are preserved, and the replacement is then validated through the ordinary `PrepareReplaceSectionBody` candidate proof. Arbitrary prose/lists never become raw rewrite authority merely because they resemble navigation content.

No anchor map, fragment cache, TOC cache, or second section tree is retained in `Document`. M108 measurement did not justify adding one; any future persistent navigation index requires new benchmark evidence and a separate architecture decision.

## Relationship intelligence model

M99 generalizes M93's compact parser-resolved reference-usage safety vector into one source-ordered `LinkUsage` vector covering direct/reference links, direct/reference images, and autolinks. The parser adapter contributes only Marksplice-owned scalar facts: semantic kind/form, source anchor, reference value when applicable, resolved destination/title, and autolink email classification. Recognized front-matter-envelope usages are filtered because front matter remains outside Markdown semantics in the snapshot.

`Document.LinkRelationships` is a read-only projection over that vector, not a new mutation model. A relationship may exist even when its source shape is outside the narrower editable public node subset. `SourceNodeID` is exposed only when the exact direct relationship already corresponds to an existing promoted node. `ReferenceDefinitionID` is exposed only when parser-defined normalized label plus resolved destination/title facts identify exactly one existing promoted definition; semantically valid but unpromoted definitions still resolve the relationship without gaining a fabricated public owner identity.

Only semantic destinations beginning with `#` are resolved inside the current snapshot. M99 reuses the M98 fragment normalization/target model through one ephemeral fragment catalog per relationship-enumeration call; other-document paths and external URLs remain opaque relationship data. Unresolved reference-looking source is not reinterpreted lexically as a relationship; that diagnostic boundary remains M101.

No normalized-definition map, fragment catalog, relationship index, backlink map, or graph is retained in `Document`. M99 builds ephemeral source-node/definition/fragment maps once per enumeration call so projection is expected O(N+H+R) rather than all-pairs. The authoritative retained relationship state remains the source-ordered `linkUsages` scalar vector.

## Multi-document graph model

M100 introduces `DocumentGraph` as a separate immutable aggregate over an **explicit caller-provided set** of already parsed `Document` snapshots. The graph does not discover documents. Callers assign opaque `DocumentKey` values and may provide a build-only `DocumentResolver` that maps one non-local M99 `LinkRelationship` to a `DocumentKey` already present in the input set plus an optional target fragment. Keys and destinations are data: Marksplice performs no path normalization, directory traversal, file reads, URL fetching, or network access.

A semantic destination beginning with `#` is always local to its source snapshot and becomes a self-edge without invoking the resolver. Every other relationship is admitted only when the resolver explicitly returns true. A successful resolution to an empty or unknown key fails the complete graph build with `ErrInvalidGraph`; returning false simply leaves that relationship outside the resolved graph. The resolver is never retained, so graph reads cannot trigger later policy callbacks or I/O.

`DocumentGraph` stores each resolved `GraphEdge` exactly once in deterministic document/source order. Outgoing and backlink adjacency store integer edge indices rather than duplicate edge values. Repeated relationships remain repeated edges because source occurrence multiplicity is semantic graph data. `ReachableFrom` is iterative breadth-first traversal with visited-before-enqueue cycle handling; `RelatedDocuments` is the unique direct incoming/outgoing neighbor set returned in caller document order. Public variable-length results are defensive copies.

Cross-document fragments reuse M98, but without rebuilding the same target catalog for every edge. During graph construction M100 creates a fragment resolver lazily for each target document that actually receives a resolver-provided fragment; that closure captures one ephemeral M98 fragment catalog and is discarded when `BuildDocumentGraph` returns. `DocumentGraph` retains only the resolved optional `FragmentTarget`, never the catalog or resolver. Missing/ambiguous/malformed fragments therefore keep their document edge but have no `FragmentTarget`.

## Workspace validation model

M101 layers diagnostics and conservative repair planning over M98–M100 without turning Marksplice into a workspace crawler. `ValidateWorkspace` receives the complete explicit `GraphDocument` set, optional caller roots, explicit managed-TOC targets, and a build-time `WorkspaceResolver`. The resolver classifies each non-local M99 relationship as `Ignore`, `Resolved`, or `Missing`; Marksplice never derives path semantics from a destination and never retains the resolver after validation.

`WorkspaceReport` combines the resolved immutable M100 graph, defensive-copy diagnostics, and a repair plan. Source-bound findings carry the caller's `DocumentKey` plus exact source offset when one exists; orphan findings are target-only because no single source occurrence causes reachability failure. Local fragment findings reuse the existing M99/M98 status directly. Cross-document fragments use the same build-local target fragment catalogs as graph construction, and caller `Resolved` decisions are cached for the duration of validation so user policy is never invoked twice.

Unresolved references require one additional parser-independent scalar observation because a failed reference parse leaves no M99 relationship. The Native backend records only conservative explicit full/collapsed unresolved link/image forms from eligible text runs, using the same document-scoped reference definitions and label normalization as resolved relationships. Escaped, code/raw-HTML-owned, multiline/complex, front-matter, and shortcut-only bracket text are excluded rather than guessed. The immutable snapshot retains only the compact source-ordered unresolved-reference vector; no parser context or secondary AST survives parsing.

Root-relative orphan detection uses one private multi-source BFS over M100 adjacency, O(V+E) regardless of root count. Managed TOC ownership/status is batched per document; heading sets and generated TOC bytes are call-local. Repair authority is intentionally narrower than diagnostics: M101 automatically repairs only caller-designated M98-recognized stale TOCs. Multiple stale TOCs in one snapshot use one family-specific atomic multi-patch candidate proof and yield at most one ordinary snapshot-bound `ChangeSet` per document. Generic M95 `ComposeChanges` is not weakened when independently prepared generated-index changes overlap semantically.

## Knowledge-document model

M107 introduces `KnowledgeIndex` as an immutable syntax-independent semantic overlay on an existing M100 `DocumentGraph`. The M100 graph remains the complete document and authority boundary: metadata may cover any subset of existing graph documents, aliases/tags are exact caller-provided scalar data, and logical references name existing `DocumentKey` targets directly. Marksplice does not infer these facts from wikilinks, hashtags, front matter, headings, filenames, paths, or URLs, and M107 adds no resolver, parser observation, source mutation, filesystem/network access, or hidden discovery.

`KnowledgeIndex` retains one pointer to the authoritative immutable graph rather than copying documents, M99 relationships, or M100 edges. Per-document state contains defensive copies of aliases and tags. The build-time global alias-owner map is retained for expected O(1) exact alias lookup. Logical references are stored once in graph-document order plus outgoing/backlink maps of integer reference indices; input target slices are build-local validation state and are not retained a second time.

Source-backed M100 `GraphEdge` values and caller-declared M107 `KnowledgeReference` values remain distinct relationship families. Logical references have no fabricated source offset or mutation authority and never appear in `DocumentGraph.Edges()`. Combined reachability performs deterministic BFS using M100 outgoing edges first and M107 logical references second for each visited document, while shared visited-before-enqueue handling deduplicates self/cyclic paths and targets present in both families. Combined related-document queries mark both relationship families and project the unique neighbor set in authoritative M100 graph-input order.

## Semantic block pattern model

M102 treats GitHub alerts as a Marksplice-owned semantic overlay over already-promoted top-level blockquotes rather than as a new parser grammar or structural node kind. `Alert.ID()` is the underlying blockquote `NodeID`; `Alert.Range()` is the existing complete blockquote source range; the exact first M94 inner range supplies the reviewed uppercase alert marker; and body ranges are defensive copies of the remaining M94 physical inner ranges. Recognition is recomputed in source order when requested and stores no alert index or parser state in `Document`.

Construction keeps alert intent distinct from generic private blockquote intent so an alert cannot later be admitted as a child through `AppendBlockquoteBlocks` or another alert. The alert writer reuses the canonical depth-1 blockquote writer, and multi-block alerts reuse the existing child-builder snapshot plus blockquote inner-source proof. Candidate validation first proves the ordinary blockquote hierarchy/source mapping, then checks the exact expected marker and source mapping. No separate alert grammar mode, public `Kind`, identity namespace, renderer behavior, or alert-specific existing-source mutation authority is introduced.

## Fenced-block model

M103 separates complete fenced-container readability from the historical contiguous payload-mutation contract. `FencedBlock` is an immutable read-only view over one source-proven top-level GFM fenced container and shares the existing fenced-code snapshot identity rather than creating a second public `Kind` or identity namespace. Its complete range, opening/optional closing delimiter runs, indentation, parser-proven info string/language, and per-physical-line payload ranges are source facts; empty and unclosed forms are represented without synthetic payload bytes.

Native owns fenced-code grammar, opening-fence anchors, body segmentation, info semantics, and language derivation directly. Parser observations remain evidence rather than source ownership: `internal/source` independently proves the opening fence, exact info bytes, every parser-backed physical payload line, optional matching closing fence, and complete container boundary against the immutable source snapshot. Parser/source disagreement fails closed.

The historical `FencedCode` view and `PrepareReplaceFencedCode` remain available only when the semantic payload is one exact contiguous source span. Broader `FencedBlock` readability therefore does not authorize generic rewrites of empty or non-contiguous payloads. Construction may emit an empty fenced body canonically with adjacent opening/closing fence lines, but embedded payload languages remain opaque data: core performs no embedded-language parsing, validation, execution, highlighting, or rendering.

Fenced-block proof is O(F) in the physical lines owned by the container and retains at most one source-backed payload range per semantic body line. Enumeration scans the existing source-ordered node collection and adds no persistent fenced-block index, language registry, secondary AST, or embedded-language parser state.

## Footnote model

M104 treats footnotes as an explicitly reviewed Marksplice core capability outside the pinned GFM 0.29 baseline. Native integrates the reviewed footnote observation pass with the ordinary document parse while keeping the capability distinct from the normative GFM grammar. No caller-selectable dialect switch, secondary parser dependency, or persistent parser state is added.

The footnote pass records only Marksplice-owned source facts: top-level definition anchor/label/body ranges, exact reference token/label ranges, definition anchor, occurrence order, and ordinary link usages nested inside a footnote definition. Before the immutable snapshot is built, source regions claimed by parser-proven footnote definitions suppress competing baseline GFM nodes/link usages/unresolved-reference observations only inside those regions. Claimed intervals are sorted and overlap-normalized once; containment checks use ordered lookup rather than definition-by-observation all-pairs scans. Baseline GFM observations outside claimed regions remain unchanged.

`internal/source` independently proves complete top-level definition ownership: at most three leading ordinary spaces, exact `[^label]:` bytes, the complete opening line plus supported blank/indented continuation lines, and every parser-proven semantic body segment contained within that physical owner. `FootnoteDefinition.Range()` exposes the complete proven container, while editable `BodyRange()` exists only for one non-empty semantic span on the opening line. Multiline/segmented definitions therefore remain readable through caller-owned body ranges without gaining broad rewrite authority.

The immutable snapshot stores promoted definitions in the ordinary source-ordered node vector plus one compact source-ordered `FootnoteReference` relationship vector. Reference-to-definition matching is exact and case-sensitive; occurrence values are zero-based in source order for the same parser-resolved definition. Definition IDs are resolved through an ephemeral parse-time anchor map. No persistent label map, reverse-reference index, second graph, parser AST, or parser context is retained.

M104 body replacement patches only the proven simple body span. Rename is one coordinated source-bound operation over the definition label and every parser-proven reference occurrence bound to that exact definition; candidate proof preserves definition/reference cardinality, occurrence ordering, ownership, and unrelated link relationships. `KindFootnoteDefinition` participates in M97 queries. M95 composition includes footnote relationships and can represent separated same-cardinality model changes as multiple disjoint changed runs while retaining the older conservative contiguous behavior for cardinality-changing deltas. Ordinary links/images/autolinks inside footnote bodies enter the existing M99 relationship vector and therefore M100 graph construction without a footnote-specific graph edge type.

Construction emits only canonical one-line top-level definitions. Definitions may be appended immediately or explicitly deferred; `FootnoteReferenceInline` resolves exactly one prior/deferred definition with exact case-sensitive matching. Temporary proof definitions exist only in construction validation input and never leak into requested output. Deferred footnotes are rejected from quoted child builders rather than silently discarded.

## Mathematical expression model

M105 adds mathematical expressions as an explicitly reviewed Marksplice core semantic overlay outside the pinned GFM 0.29 baseline. Native recognizes the reviewed overlay without creating a second Markdown mode, mathematical parser, or renderer. The reviewed dedicated source forms are non-empty single-line `$...$`, dollar-backtick, and top-level one-physical-line `$$...$$`; exact-info top-level fenced blocks whose parsed info is `math` are projected through the existing M103 fenced-block model.

Native observes dedicated math only inside parser-owned safe regions. Inline-dollar scanning is limited to eligible text runs that exclude code, link, image, autolink, and raw-HTML ownership. Dollar-backtick recognition is anchored to one simple parser-proven code span plus exact surrounding dollars. Block-dollar recognition requires one complete top-level paragraph occupying one physical line. The exact baseline code-span/paragraph observations displaced by those dedicated forms are suppressed only when their ranges match the reviewed math source exactly; unrelated GFM structure remains unchanged. The three source-ordered observation streams are merged linearly rather than globally sorted.

`internal/source.MapMathExpression` independently proves delimiter and payload ownership. It rejects empty or multiline dedicated payloads, escaped/adjacent ambiguous dollar delimiters, unescaped dollars inside payload, backticks inside dollar-backtick payload, and block-dollar source that is not one complete physical line. Block-dollar ownership includes its physical line terminator, including CRLF. Parser/source disagreement fails closed.

Dedicated source forms become `KindMathExpression` nodes in the ordinary source-ordered node vector. Exact-info `math` fences do not receive a second structural node: `MathExpressionFencedBlock` reuses the existing fenced `NodeID`, complete container ownership, and M103 payload ranges while `Document.Node(id)` remains `KindFencedCode`. No persistent math index, AST, renderer state, or embedded-language model is retained.

`PrepareReplaceMathExpression` patches only a source-proven payload and reparses the candidate. Dedicated forms must preserve the same mathematical style and transformed source mapping; fenced math delegates to the historical contiguous fenced-code payload replacement boundary and therefore gains no broader mutation authority. M95 composition includes math style/payload facts for dedicated nodes and deliberately still rejects two independent edits inside the same paragraph aggregate. `KindMathExpression` participates in M97 queries; fenced math remains queryable structurally as `KindFencedCode` rather than being duplicated.

Construction adds `MathInline`, `MathBacktickInline`, and canonical one-line `AppendMathBlock`. Typed math is admitted only in the ordinary top-level inline sequence, not inside unreviewed structured label/emphasis child hierarchies. Multiline mathematical construction continues to use `AppendFencedCode(..., "math")`. All mathematical payload remains opaque; Marksplice does not parse, validate, render, execute, or fetch anything for TeX/LaTeX/MathJax/KaTeX/MathML content.

## Existing-document mutation model

A prepared change contains the source fingerprint it was created against and one or more validated, non-overlapping byte patches.

Application rules are:

1. fingerprint the supplied source;
2. reject stale source explicitly;
3. validate patch ranges and ordering;
4. apply patches in source coordinates without rendering unrelated source;
5. preserve every byte outside changed ranges.

One named structural operation may own multiple coordinated patches, such as delete+insert movement. M95 additionally lets callers combine multiple **already-prepared** opaque `ChangeSet` values through `Document.ComposeChanges`, but this still is not a generic public patch API. Every constituent must be bound to the same exact snapshot; source-level patch overlap and same-offset insertion conflicts are rejected before semantic composition.

For two or more composed changes, Marksplice applies each constituent to the original snapshot to derive its independently validated compact node/link/footnote-relationship model delta, rejects overlapping logical deltas, composes the expected deltas in model/source order, then parses one combined candidate and requires the result to equal that expected composed model. Node IDs and absolute offsets are not composition identity; semantic facts, source-owned shape, relative relationships, table/list/blockquote ownership facts, and parser-resolved link relationships are. This catches cross-operation interactions such as two removals that are safe individually but together change paragraph structure. Family-specific atomic operations remain preferable when several intents deliberately change one logical model region.

When a local byte patch could change surrounding Markdown interpretation, mutation preparation reparses a candidate and validates the required surviving source/semantic model. Candidate proof is a conservative safety oracle, not permission to rewrite the document. Unsafe joins, newly introduced promoted structure, changed container relationships, ambiguous ownership, and malformed source fail closed.

No operation synthesizes whitespace, line endings, numbering, or delimiters unless that synthesis is part of an explicit new-document construction contract. Existing lexical trivia is source data.

## Section model

Sections are derived from promoted document-level headings rather than assigned a second identity namespace. A section distinguishes the governing heading, direct body, and complete subtree. Parent/child relationships derive from heading levels.

The section hierarchy is built once from source-ordered headings using a monotonic stack. Heading `NodeID` remains the section identity. Immediate child navigation reuses the same hierarchy with compact scalar linkage rather than storing slices inside the comparable public `Section` value.

Section mutations operate on exact stored boundaries. Body replacement preserves the heading and child sections; subtree replacement/insertion/movement validate caller or snapshot-owned section structure; direct-child append requires exactly parent level + 1. Structural candidate validation preserves unaffected heading level/style/source mapping and rejects Setext/paragraph or other join reinterpretation. No operation rewrites heading levels or manufactures separator whitespace.

## List model

Promoted list items retain exact first-line content replacement semantics while internal source ownership records the complete physical line. Supported single-line-head parent items may expose semantic `HasChildren`, immediate supported `ParentID`/`ChildIDs`, and `SubtreeRange()` only when the complete supported descendant subtree is proven.

Parent identities are resolved after ordinary snapshot IDs exist. Downward navigation uses compact source-ordered adjacency. Temporary anchor/ordinal maps are discarded after parse; no second persistent hierarchy index is introduced.

Subtree completeness compares semantic direct-child counts with supported promoted children and propagates completeness leaf-up. Structural removal, movement, sibling insertion, child append, and subtree replacement require the relevant complete source-owned subtree. Unsupported descendants make the structural boundary unavailable rather than guessed.

Host context is authoritative where GFM indentation depends on parent marker width and container context. Child insertion/replacement is therefore validated in the host candidate instead of forcing standalone parseability. Marksplice never synthesizes indentation, list numbering, markers, or line endings for existing source.

Movement uses exact snapshot-owned subtree bytes and coordinated original-coordinate patches. Overlapping source/anchor subtrees are rejected. Same-parent no-ops remain fingerprint-bound. Candidate proof validates descendant-relative mappings, parentage, source bytes, and operation-known parent child-count deltas.

## Table model

Tables have a source-proven public identity independent of any body row. `Table.Range()` owns the complete header + delimiter + semantic body source block. `ColumnCount()` is the source-proven semantic width; `BodyRowCount()` is semantic, may be zero, and can exceed the number of promoted body-row identities.

Native provides semantic table hierarchy/alignment values, while Marksplice derives lexical ownership independently. The source table anchor comes from the parser-proven header position rather than trusting a synthetic container position. Header, delimiter, body rows, cell ranges, and delimiter alignment tokens are mapped in the source layer.

Public rows/cells keep comparable scalar values. Variable-length table relationships and alignment vectors are document accessors returning caller-owned storage. Parse-time temporary maps build compact row/header/cell adjacency and scalar table ownership; persistent anchor maps are avoided.

Row mutations preserve exact caller/source row bytes, validate compatible width/table ownership in the host candidate, and never synthesize a missing line separator. Row movement is restricted to the same table under the current contract.

Alignment mutation changes only source-proven delimiter colons while preserving each exact dash run and surrounding trivia. Whole-vector alignment mutation batches changed delimiter tokens into one candidate validation cycle.

Column insert/remove/move uses a stricter operation-time capability gate: header, delimiter, and every semantic body row must all map at exactly the table width. Removal owns one complete slot plus an adjacent separator; movement permutes source-proven contents while destination-slot trivia remains fixed; insertion clones adjacent slot padding/separators and delimiter dash-run style. Target-table rows/cells may structurally change, while non-target table survivors still undergo ordinary transformed-range/source validation. These operations add no persistent column index.

## Blocks, inline syntax, metadata, and HTML

Paragraphs/headings, fenced blocks/code, simple strikethrough/code/emphasis/strong, links/reference definitions/autolinks/images, simple front-matter fields, and narrow HTML comment/anchor forms follow one mapped-capability rule: parser semantics identify the family, then Marksplice proves exact operation-oriented source ownership before public editability.

M103 exposes complete fenced-container metadata through the read-only `FencedBlock` model after independent source proof, while fenced-code payload replacement remains limited to the historical exact contiguous `FencedCode` span. Exact delimiter runs, indentation, info source, complete container ownership, closure state, and per-line payload source are therefore public/readable where proven without becoming generic mutation spans. Simple inline spans reject shapes that require normalization or ambiguous delimiter ownership. Link/image/reference/autolink mutations preserve labels, wrappers, titles, spacing, and other source trivia outside the exact destination/token range. M96 allows promoted simple `InlineLink`, `ReferenceDefinition`, and `AutoLink` values to expose scalar semantic facts that were already parser-proven and stored internally: link destination/optional title, definition label/destination/optional title, and autolink value/email classification. These getters do not redefine the operation-oriented `Range()` contracts or widen parsed-source promotion. M93 extends promoted single-line reference-definition ownership with an internal complete physical `LineRange`; M99 generalizes the same parser-resolved safety metadata across direct/reference links, images, and autolinks and exposes it only as immutable relationship intelligence. Complete-line removal and composition now preserve every surviving link relationship, while reference/direct shapes outside ordinary edit promotion remain non-editable.

Thematic breaks own one complete physical line. M94 blockquotes instead own one complete top-level physical container across source-proven multiline, nested, lazy-continuation, marker-only, and multi-block forms. The public `Blockquote.Range()` is the complete owned source span; `Document.BlockquoteContentRanges` returns caller-owned inner ranges for each owned physical line, preserving nested markers as inner source and representing marker-only lines with valid empty ranges. The historical single-paragraph `ContentRange()` remains available only when that exact old contract is parser-proven; broader forms return the zero range rather than a synthetic span across markers. A markerless lazy line is owned only when a parser semantic block range proves content on that exact physical line. Nested blockquotes remain semantic children of one promoted outer top-level identity rather than gaining separate public identities. Complete-container removal uses the shared whole-block survivor proof plus transformed blockquote line/marker/content-range validation so paragraph/Setext/container joins and surviving blockquote ownership cannot silently change.

Front matter is a Marksplice-owned document envelope outside the GFM parser. M106 separates envelope ownership from field editability. A closed byte-zero `---` YAML or `+++` TOML envelope is recognized when it is empty or contains conservative metadata evidence: a simple top-level YAML/TOML key separator, or for TOML a complete conservative simple table/array-table header. Empty leading envelopes have explicit metadata precedence; a non-empty delimiter pair with no metadata evidence, a non-leading form, or an unclosed form remains ordinary GFM source. `Document.FrontMatter()` exposes only immutable format and complete/opening/closing source ranges; it is not a structural node or metadata AST. Complex and duplicate content remains opaque. Only unique source-proven simple top-level scalar fields are promoted for existing-document replacement, and TOML field promotion stops when table scope begins. Existing-document edits preserve all other envelope bytes. New-document construction remains the narrower M80 contract: at most one envelope as separate `DocumentBuilder` state with conservative canonical double-quoted string fields before every GFM body block.

Existing-source HTML editing remains conservative. Native recognizes reviewed raw/block HTML semantics, but public edits are limited to source-proven simple comment payloads and quoted `id`/`name` values on supported `<a>` openings. M120 rendering is broader because read/render semantics do not grant mutation authority: parser-proven raw/block HTML can be preserved or escaped through explicit `HTMLRenderOptions`, with optional GFM tag filtering. Preserved raw HTML remains active markup rather than sanitized content; applications must choose `HTMLRawEscape` or a downstream sanitization boundary for untrusted input. M121 standalone HTML reuses those body semantics exactly and maps only `title`, `description`, `author`, and `lang` from already-promoted unique top-level simple front-matter fields. Escape-dependent/complex metadata stays opaque and is omitted rather than interpreted.

## New-document construction model

`DocumentBuilder` is deliberately separate from parsed `Document` snapshots. Construction state represents intent before authoritative source exists, so it has no snapshot `NodeID`, source fingerprint, or stale-source semantics.

The builder writes deterministic LF GFM, one blank line between retained GFM blocks, and one final LF for a non-empty document. An optional YAML/TOML front-matter envelope is separate document-leading state rather than a `constructionBlock`; when a body exists, exactly one blank line separates the closing envelope delimiter from the first GFM block. Each requested block is validated, written, reparsed, and checked against family-specific semantic/source expectations before retention; `Markdown()` validates the complete generated sequence again.

Historical block/list/table builder APIs accept caller-provided raw GFM where documented. Typed-inline APIs are a separate semantic-text path and do not change those raw-source contracts.

Canonical construction currently includes a single optional YAML/TOML front-matter envelope with conservative string fields, ATX headings, parser-proven paragraphs, thematic breaks, single-paragraph blockquotes at depth 1 or explicit nesting depth 2–64 with canonical repeated `> ` prefixes on every LF-separated physical line, multi-block blockquotes at depth 1–64 composed from every reviewed body-block family including recursively nested blockquotes with total structural depth bounded at 64, exact depth-1 GitHub alerts from single-paragraph/typed-inline or reviewed multi-block bodies, flat/homogeneous nested ordered/unordered lists and task lists, fenced code including canonical empty payloads, ordinary reference definitions, canonical one-line immediate/deferred footnote definitions, canonical one-line block-dollar mathematical expressions, and GFM tables with optional explicit alignment and optional body rows.

Nested list construction accepts structural depth rather than source indentation. The writer derives indentation from the generated parent content column and uses container-local ordered numbering. Fenced-code construction selects adaptive backtick fences against every potentially closing body run. Reference definitions use angle destinations and a conservative no-escape title form. Tables use canonical outer pipes/padding and canonical alignment delimiters. M96 gives every generated table an explicit complete-table construction expectation, so a header+delimiter table with zero body rows is accepted only after the same table anchor/range/width/alignment/source mapping is proven; body-row expectations remain additive when rows exist.

Typed inline construction represents semantic text and reviewed inline structures rather than raw Markdown injection. Text is deterministically escaped. Code uses adaptive backticks under the existing exact source-mapped code-span boundary. M88 permits bounded structured nesting of code, emphasis, strong, and strikethrough inside emphasis/strong/strikethrough wrappers. Link/image typed construction uses strict canonical angle destinations; M87 adds separate `WithTitle` constructors that write conservative non-empty double-quoted titles and prove exact title presence/value/source range through the existing mappings. M89 adds canonical full reference-link/reference-image construction whose exact reference label must match exactly one already-appended top-level reference definition in the same builder. M92 lets direct and full-reference link labels/image alt content reuse the M88 structured child family while keeping destination/title/reference semantic proof separate from child hierarchy proof. M93 adds explicit deferred top-level ordinary reference definitions plus `ForwardReference*Inline` for full forward references, parser-normalized collapsed/shortcut reference link/image forms, and `BareAutoLinkInline` for exact parser-proven non-angle autolink tokens. M104 adds a separate exact-label footnote construction family: immediate/deferred canonical definitions plus `FootnoteReferenceInline`; caret-prefixed ordinary reference-definition labels are reserved to avoid ambiguous builder output. M105 adds top-level typed `MathInline`/`MathBacktickInline` construction and canonical `AppendMathBlock`; mathematical typed values are deliberately excluded from the existing structured-child family until that hierarchy is explicitly reviewed. Historical M89 constructors still consult only already-appended ordinary definitions; normalized ambiguity and parser-truncated bare tokens fail closed. Nested links/images/autolinks/references/footnote references/math inside structured labels remain rejected, and ambiguous delimiter hierarchies still fail closed.

Construction proof reuses the same parser-independent source mappings and semantic metadata as parsed snapshots rather than creating a parallel Markdown AST. Front-matter construction specifically reuses `MapLeadingFrontMatter`; body expectations are written into the already-prefixed final buffer so their byte ranges are final rather than shifted after envelope insertion. M81–M86 blockquote construction deliberately established construction-only semantic validators plus Marksplice lexical proof while ordinary parsed-source blockquotes were still narrow. M94 later broadens ordinary top-level blockquote promotion through a separate physical-ownership proof; the construction validators remain independent, so the ability to parse or construct a broader blockquote never by itself authorizes a different construction shape or existing-source edit. M82 proves explicit single-paragraph structural depth. M83 snapshots a child `DocumentBuilder`, renders its already-reviewed blocks through the ordinary construction writers, prefixes every physical line including canonical blank separators, and proves that removing those prefixes yields the exact canonical inner source. M84–M85 extend the construction-only parser comparison to list/list-item hierarchies, reference definitions, and table/header/row/cell hierarchies with alignment values. M86 admits blockquote nodes into that same iterative child comparator and performs an explicit iterative pre-render traversal of the private construction tree so every nested blockquote chain remains within the 64-level bound before recursive canonical writing can occur. Observations wholly contained by a construction-only multi-block blockquote are excluded from ordinary construction-node matching because the container-specific proof is authoritative there. Front matter remains a document envelope. M87 extends the existing typed-inline expectation proof with title value/range facts only; it adds no parser/source mapper. M88 adds a separate ephemeral construction-inline hierarchy proof for code/emphasis/strong/strikethrough wrappers rather than widening ordinary simple-inline promotion. The writer bounds wrapper nesting at 64, alternates `*`/`_` for nested emphasis-family delimiters, rejects direct strikethrough-in-strikethrough, and still fails closed when GFM reparsing cannot reproduce an exact requested hierarchy. Native construction matching is single-pass with anchor buckets and precomputed parent→children lists, while simple leaf/source-mapped expectations remain independently validated through the existing parsed snapshot path. M89 reuses the same construction-only boundary for full reference links/images: the builder resolves one exact existing definition, emits the requested reference token, and validates it against an ephemeral proof source containing canonical definitions. The proof requires the Native full-reference form, exact reference value, resolved destination/title semantics, and exact label/reference source ranges; the proof definitions are never emitted into the requested block and ordinary parsed-source link/image promotion remains unchanged. M92 extends the hierarchy proof so a direct or full-reference link/image can own reviewed structured children, but does not move destination/title/reference facts into that hierarchy contract: direct link/image semantics use a separate construction-only expectation, full references retain the M89 semantic expectation, and the hierarchy validator receives reference facts only to synthesize the ephemeral definitions required to parse the requested reference owner. M93 keeps GFM reference-label normalization behind the parser-independent backend boundary, separates prior and deferred builder scopes, and extends construction-only reference expectations with full/collapsed/shortcut source form. For existing-source removal safety the same ordinary parser observation pass also records a compact Marksplice-owned vector of non-public reference usages; recognized front-matter-envelope anchors are filtered because front matter is not Markdown in the Marksplice model. Whole-block removal compares only surviving usages with transformed anchors, so an owned block may remove its own relationships while external relationships cannot silently change. Ordinary public parsed reference-link/image promotion remains unchanged. Successful generated source may then be passed to `Parse` to enter the immutable snapshot/editing model.

## GFM and parser boundary

`docs/gfm-conformance.md` owns the normative source hierarchy, pinned CommonMark/GFM snapshots, Native parser-neutral conformance procedure, and update policy. CommonMark 0.31.2 is the authoritative base grammar; the published GFM specification adds explicit extension/correction behavior. Marksplice exposes no separate CommonMark mode.

`internal/parser/native` is the production semantic parser. Parser implementation behavior is never a normative source: regressions are classified against CommonMark 0.31.2, explicit GFM extensions/corrections, or an approved Marksplice-owned capability. Parser-internal types do not cross the public API, and ordinary existing-document edits never serialize a parser AST or regenerate untouched Markdown. M102 alerts, M104 footnotes, and M105 math remain explicit Marksplice-owned capabilities rather than hidden dialect switches or third-party extension modes.

Where semantic observations are insufficient for lossless source work, Marksplice may perform bounded lexical scans tied to the immutable source snapshot. Such scans remain parser-independent and internal; semantic recognition does not by itself grant mutation ownership.

YAML/TOML front matter remains a separate document-envelope layer, not an additional Markdown dialect. M120 HTML fragment rendering and M121 standalone wrapping are explicit export paths over the on-demand semantic layer and remain separate from ordinary source-preserving edits. Rendering-specific requirements such as GFM tag filtering are covered by independent full-profile expected-HTML gates; parser-neutral conformance remains separate. Canonical Markdown rendering is likewise an explicit future export path and must never replace ordinary source-preserving edits.

### Core capability and third-party extensibility boundary

`docs/extension-strategy.md` owns the feature-selection and extensibility policy. Marksplice does not maintain a collection of first-party syntax extensions.

Broadly useful capabilities belong directly in core when their contract is general enough: examples include heading anchors/TOC, semantic patterns over already-valid Markdown, generic fenced-block metadata, and other document/navigation primitives. Dialect-specific spellings such as wikilinks, hashtags, definition lists, custom containers, emoji shortcodes, or product-specific Markdown stay outside core unless a later explicit core decision establishes broad value.

M110 exposes a small public read-only SPI so independent, statically linked Go packages can attach dialect-specific syntax or semantics without entering the core grammar. The overlay is evaluated only after baseline parsing and cannot redefine GFM semantics, bypass stale-source/patch validation, gain Marksplice mutation/construction/host authority, or expose parser-specific types. M111–M115 preserved this parser-backend-independent boundary throughout parser replacement.

### Native parser ownership after M115

M111 froze the substitution boundary as the internal `parser.Backend`: one complete `DocumentObservations` pass plus construction-only blockquote/typed-inline/link/reference proof and reference-normalization primitives consumed by `internal/splice`. M112 implemented Native block parsing, M113 implemented Native inline/reference parsing, and M114 completed the construction-proof surface plus specification-first hardening before production cutover.

M115 switched the single production bridge to `native.New()` and the same Native reference-label normalization. The first black-box cutover exposed one previously frozen public table contract not covered by the published corpus: a valid table header immediately following an open paragraph line must split that trailing line from the paragraph. A focused Native regression was added before the parser fix, and the full public/root/splice suite then passed with production actually using Native.

Before deleting the historical comparator, M115 froze Marksplice-owned parser-neutral expected observations for all 652 CommonMark examples and 676 parser-applicable published-GFM examples. The fixture entries are bound to official example identity and the SHA-256 of externally provisioned Markdown input. A dual-proof gate demonstrated that those fixtures matched Native while the previously validated pre-removal reference/differential gates were still green. Only then were the former Goldmark adapter, differential harness, parity-only Native tests, and `github.com/yuin/goldmark` module dependency removed. Current conformance uses the Native fixtures plus hash-pinned external specification snapshots; future fixture updates cannot be accepted merely by serializing current Native output.

M114 remains the pre-cutover hardening record: its Native-only real-world campaign reached three stable post-defect expansion waves over an exact-byte-certified corpus of 195 repositories / 6,857 Markdown files / 60,778,570 bytes, with deterministic source-bound parsing and malformed derivatives plus resource/performance/cross-platform/security evidence. Historical differential and renderer results remain engineering history only. M115 completes the parser replacement roadmap with production and active tests Native-only.

## Line endings, Unicode, and encoding

Existing-source edits must not normalize LF, CRLF, isolated CR, Unicode content, or unrelated byte sequences. If parser compatibility requires a same-length shadow view, byte offsets must remain exactly aligned with original source.

Encoding/BOM preservation belongs to the host that supplies Markdown bytes unless Marksplice later introduces an explicit encoding-aware layer. Core must not guess and rewrite file encodings. Native GFM reference-label keys use the direct `golang.org/x/text/cases` dependency for full Unicode case folding, while GFM whitespace collapse remains an explicit ASCII-whitespace rule. `golang.org/x/text` remains the parser-owned dependency after M115.

New-document construction currently emits canonical LF by contract.

## Performance and complexity

Parsing/index construction should remain linear or near-linear in source size where practical. Relationship models use compact source-ordered arrays/scalar links plus temporary maps rather than redundant persistent indexes. Mutation planning should avoid repeated whole-document rescans beyond what the operation's safety proof explicitly requires. M95 composition currently performs O(k·N) proof work for `k` supplied prepared changes over a document/model of size `N`: one independently prepared candidate comparison per constituent plus one combined candidate parse. It uses no all-pairs node matching, no repeated list-subtree hashing, and no persistent composition cache/index. M97 node/section queries are O(N) worst-case scans with early stop at the explicit caller limit and O(limit) result memory; they add no persistent kind/range index or result sort. M98 heading-anchor/TOC derivation is O(H) expected for `H` headings with O(H) temporary/result state; fragment resolution is O(H+N) over headings and existing nodes, and managed-TOC recognition is O(B) over the designated body bytes. M99 `LinkRelationships` builds ephemeral source-node, normalized-definition-owner, and fragment maps once, then projects `R` relationships in expected O(N+H+R) time and O(N+H+R) temporary/result memory; it retains no graph/index. M100 graph construction is O(V+R+E) plus M99 projection/resolver cost and at most one additional ephemeral fragment-catalog build per target document that receives cross-document fragments; persistent graph state is O(V+E) with one edge vector plus adjacency indices. `ReachableFrom` is O(V+E) worst case and `RelatedDocuments` is O(V+degree). M101 validation invokes caller resolution once per non-local relationship, computes all root reachability with one O(V+E) multi-source BFS, batches managed-heading ownership and generated TOC bytes per document/EOL, and plans multiple stale TOCs with O(T log T) patch ordering plus one combined candidate parse/proof per repaired document. Resolver callbacks, fragment catalogs, heading sets, traversal state, and TOC caches are call-local only. M102 alert enumeration is O(N+L) worst case over promoted nodes and inspected blockquote content-range entries, with O(A) caller-owned result storage and no persistent semantic index. M103 fenced-block source proof is O(F) in the owned physical fence/body lines and stores at most one source-backed range per semantic payload line; `FencedBlocks` reuses the existing source-ordered node scan and retains no persistent fence or language index. M104's Native footnote pass normalizes claimed definition regions once and queries them by ordered interval lookup, while retained snapshot state is limited to promoted definition nodes plus one compact source-ordered footnote-reference vector and no persistent label/reverse-reference index. M105 adds a constant number of bounded math observation passes over parser-safe source regions, linearly merges already-source-ordered results, and retains only promoted dedicated math nodes while fenced math reuses M103 state; no persistent math index or rendering state is added. M106 keeps front-matter recognition source-linear: one bounded closing-delimiter scan plus one bounded physical-line metadata/field scan, with a temporary key-count map for unique safe fields. The snapshot retains only the compact envelope boundaries already needed by mutation validation plus promoted safe fields; there is no metadata AST, parsed-value tree, schema, persistent key index, or serializer state. M107 knowledge-index build is expected O(V+A+T+L) over graph documents, aliases, tags, and logical references; persistent overlay state is O(A+T+L) plus one alias lookup and reference adjacency indices over the existing M100 graph. Exact alias lookup is expected O(1), tag lookup remains an O(V+T) scan without a second persistent tag index, combined reachability is O(V+E+L), and combined related-document queries are O(V+incident E+incident L). M110 adds one O(N) immutable source-string copy only when extensions are enabled; Marksplice-owned validation/copying is O(E+M) for retained extension nodes/metadata and retains O(E+M) overlay state, while recognizer execution cost remains caller-extension-defined. M108 measures and hardens realistic mutation, query, navigation, relationship, graph, workspace-validation, semantic-pattern, fenced-block, footnote, mathematical-expression, metadata-envelope, and knowledge-overlay workloads. Regression budgets are expressed primarily as scaling ratios rather than absolute wall-clock CI thresholds: realistic and delimiter-heavy parse workloads, duplicate-heading navigation, dense relationship projection, graph/workspace/knowledge operations, local mutation planning, and multi-change composition all have explicit 4x-input envelopes recorded in the M108 milestone. Allocation counts/bytes are treated as the more stable resource signal; host-dependent benchmark time remains review evidence rather than a flaky unit-test oracle. Historical pre-M115 Goldmark measurements remain only as comparison evidence; current pathological/resource gates measure the Native production backend directly so parser superlinearity cannot be hidden behind an arbitrary cap.

M108 profiling removed one Marksplice-owned quadratic code-span source scan and three large duplicate-copy paths. Simple code-span source proof no longer rescans complete physical lines for every delimiter on a long line; supplemental math/footnote node streams reuse parse-reserved node capacity through a stable backwards merge; section construction keeps only heading ID/level/range rather than cloning complete nodes; and terminal math/footnote parser-observation conflict filters compact source-ordered slices in place and clear removed tails. A one-MiB synthetic mixed-syntax parse consequently reduces allocation from roughly 657 MB/op to roughly 374 MB/op in the recorded production baseline, while the pathological 256-KiB dense-delimiter case improves by roughly 24–30x and returns to near-linear scaling. No query/navigation/tag index, line cache, or parser size cap is introduced by those changes. M111 adds no production comparison state or second AST. M112 adds compact physical-line native block parsing and M113 adds monotonic/indexed inline/reference parsing. M114 then reuses one per-block inline analysis, delays heavy inline-owner materialization, adds single-segment fast paths to inline/delimiter indexes, replaces reflection-based stable sorting with typed standard-library sorting, and finally reserves block-node capacity in the merged inline result so the already-ordered final document merge reuses the same backing vector instead of allocating a second full node array. On the recorded pre-cutover realistic 1 MiB workload the hardened Native path was about 180–203 ms / 214 MB / 876k allocs versus about 330–344 ms / 374 MB / 912k allocs for the then-production Goldmark path on the same host. The final 60.8 MB real-world corpus parses at about 18.8–19.2 MB/s with roughly 41.7 allocated bytes per input byte, and the three post-defect expansion waves expose no new parser-owned amplification. No persistent parse cache/index or arbitrary size cap is introduced. M115 makes that Native path the production path.

The pre-M116 v0.5 performance campaign re-baselined and then optimized the complete public `Parse` path as well as Native in isolation. Five same-host passes over the byte-certified 6,857-document / 60.8 MB corpus moved public `marksplice.Parse` from about 15.04 MB/s / 4.49 GB/op / 13.05 M allocs/op to **25.06 MB/s / 2.702 GB/op / 10.402 M allocs/op**, while Native moved from about 19.23 MB/s / 2.54 GB/op / 10.62 M allocs/op to **30.87 MB/s / 1.902 GB/op / 9.015 M allocs/op**. This is approximately +66.6%/+60.5% throughput and -39.8%/-25.1% allocated bytes for public/Native respectively, with allocation counts reduced by about 20.3%/15.1%. These are same-harness engineering ratios, not cross-machine guarantees.

The accepted v0.5 changes compact common internal node/parser-observation storage through sparse detail sidecars and narrower common records, reduce transient projection/index state, reuse source-ordered/monotonic scans, and add measured fast paths for Native inline/source mapping without adding persistent caches or a second semantic model. Parser `Node` storage and Marksplice's broad internal snapshot `Node` footprint were both materially reduced while public exported behavior remained unchanged. The campaign also hardened identity mutation semantics: once a target is valid and source-proven, a byte-identical replacement produces a snapshot-bound no-op `ChangeSet` rather than being rejected by validators intended for genuinely new content; different invalid payloads still fail closed.

A private comparison harness continues to retain Goldmark v2 only as external historical/performance evidence. Goldmark remains absent from production dependencies and normative semantics. Final acceptance used the six V05 ratio gates, the retained M108 pathological/scaling matrix, exact CommonMark/GFM conformance, all 6,857 corpus documents, malformed derivatives, fuzzing, race/static/security/complexity checks, supported-Go verification, and cross-platform builds. No benchmark-specific cache, hidden size cap, or Goldmark-derived semantic shortcut was introduced.

M117 profiles the new filesystem adapter separately rather than optimizing it by assumption. The URI-path resolver adds no measurable allocation-count regression in the frozen 256-document follow workloads. Profiling then justified one narrow traversal refactor: `Follow` uses an index-based breadth-first queue and one operation-local availability map whose key presence distinguishes unchecked targets from cached present/missing results. On the same 256-document harness this reduced follow allocation bytes/counts while leaving chain timing within noise and improving the dense workload; 256-to-1024 document scaling for 4x input measured about 4.69x for `Scan`, 4.22x for chained `Follow`, and 4.31x for dense `Follow`, with allocated bytes and counts approximately proportional to input. Parser/document construction remains the dominant cost, so M117 adds no persistent workspace cache or secondary index.

M118 keeps rendering support off the ordinary parse path and measures both boundaries. On the same frozen 256 KiB realistic harness, public `Parse` remains at approximately 53.20 MB/op / 231,672 allocations and Native `ParseDocument` at approximately 38.30 MB/op / 196,940 allocations before and after M118; same-host wall-clock movement was similar on both paths and is treated as benchmark noise rather than retained semantic cost. The on-demand semantic walk itself measures about 38.8 ms / 36.66 MB / 224,018 allocations on the recorded host. Profiling rejected an initial near-quadratic projection that repeatedly scanned analyses/details at roughly 143–175 ms per walk; the accepted design builds one ephemeral source/list/table/fence lookup index per walk and reduces the same workload to roughly 36–42 ms without persistent caches or a second retained tree.

M119 profiles the complete semantic path rather than accepting the extra rendering facts as free. The first complete draft regressed the same 256 KiB walk to roughly 68.5 ms / 57.05 MB / 239.6k allocations; CPU evidence attributed about 31% of sampled time to repeated paragraph-containment scans. An ordered paragraph index, compact first-child/next-sibling adjacency, measured capture reservation, and inline-merge capacity cleanup reduce a stable post-refactor run to about 40.05 ms / 40.26 MB / 233k allocations. Relative to the M118 semantic freeze that is approximately +9.8% allocated bytes and +4.0% allocations while completing nested containers, list facts, indented code, reference definitions, front matter, alerts, footnotes/math, autolink destinations, task text, and multiline code spans. Later same-host public/Native allocation measurements remain close to M118 at about 53.28 MB / 232.5k and 38.38 MB / 197.8k respectively; wall-clock values are treated as host-sensitive evidence, while allocation state confirms that semantic capture remains pay-on-demand.

M120 measures renderer overhead on the same 256 KiB realistic source instead of treating streaming as automatically cheap. On the recorded Windows/amd64 Ryzen 9 5900X host, five-run medians are approximately 45.3 ms / 53.3 MB for public `Parse`, 40.6 ms / 40.8 MB for `WalkSemantic`, 52.9 ms / 41.7 MB for `RenderHTML(io.Discard)`, and 54.3 ms / 43.8 MB for the buffered `HTML` helper. The writer path therefore adds roughly 0.96 MiB of allocated bytes above the semantic walk without accumulating the complete HTML result; the convenience helper adds roughly another 2.0 MiB because it owns a growing output buffer. No persistent renderer cache, whole-document HTML buffer, or second rendering tree is retained in `Document`.

M121 measures the standalone wrapper separately on a 256 KiB realistic source with four reviewed front-matter fields. Five-run measurements place streaming `RenderHTMLDocument(io.Discard)` at roughly 41.72 MB/op and 258.4k allocations and buffered `HTMLDocument` at roughly 43.82 MB/op and 258.4k allocations. The streaming path therefore stays structurally aligned with M120; front-matter metadata scanning is bounded to the recognized leading envelope, while the buffered helper pays the expected whole-output buffer cost.

The current production complexity gate is **cyclomatic complexity 15 or lower per function**: `gocyclo -over 15 -ignore '_test\.go$' .` must report no production function. Production and test-inclusive `unparam` checks are also required. This gate is a maintainability constraint, not an instruction to fragment cohesive lexical/state-machine logic; split code when responsibility, reuse, or verification clarity improves.

The post-M79 whole-code review reduced prior complexity hotspots by separating Goldmark block/inline dispatch, list marker parsing, table row/cell survivor proof, whole-block anchor validation, table alignment planning/candidate proof, table/list adjacency validation, complete-table mapping, YAML scalar validation, and shared link/image destination-tail parsing. The post-M97 review additionally isolates reference-link/reference-image construction and proof in `builder_inline_reference.go`, keeps generic typed-inline writing in `builder_inline.go`, and separates query-specific internal projection from the general snapshot document implementation. M98 similarly keeps managed-TOC orchestration separate from its small line-scanning state machine after the initial recognizer exceeded the complexity gate. M99 removes the superseded reference-only parser aliases/wrapper and replaces its first repeated definition/fragment scans with ephemeral lookup maps. M100 reuses one shared public fragment-target conversion helper, replaces repeated cross-edge `ResolveFragment` rescans with build-local per-target fragment resolvers, and stores adjacency as edge indices rather than edge copies. M101 separates conservative unresolved-reference observation from M99 relationship semantics, replaces per-root reachability and per-target heading/TOC rescans with multi-source/batched derivation, and uses a family-specific atomic TOC repair path after generic M95 composition correctly rejected overlapping generated-link semantic deltas. M102 factors quoted-block construction dispatch and blockquote base/simple/alert proof responsibilities after the first implementation exceeded the complexity gate, while reusing one canonical inner-source writer rather than duplicating blockquote logic. M103 centralizes public/internal range conversion and reuses one already-proven opening-fence mapping path for both the complete `FencedBlock` proof and the historical contiguous `FencedCode` proof, avoiding a second opening-fence scan. M104 isolates footnote promotion from the main parse orchestrator to preserve the production complexity gate, normalizes claimed regions for ordered lookup instead of nested scans, and generalizes equal-cardinality M95 deltas into disjoint changed runs so coordinated footnote rename does not falsely claim unchanged model entries between patches. M105 then separates math delimiter proof, supplemental-node promotion, auxiliary construction dispatch, and typed-inline relationship expectation dispatch after the first implementation exceeded the complexity gate; it also replaces a global observation sort with linear merging of source-ordered streams. M106 separates front-matter lexical key/separator evidence from safe scalar mapping, centralizes unique-field filtering, and uses a small conservative TOML table-header recognizer so table scope can stop top-level field promotion without introducing a TOML parser. M107 removes duplicate retained logical-target slices, retains the already-built alias-owner map for direct lookup, shares M100 document-membership/traversal/graph-order neighbor helpers rather than copying graph algorithms, and separates M100 graph input validation from relationship projection so `buildDocumentGraph` remains orchestration rather than a complexity hotspot. M108 then removes repeated whole-line code-span scans, reuses reserved node capacity for supplemental source-order merging, replaces full-node section-heading copies with a scalar projection, and compacts terminal parser conflict vectors in place with tail clearing. These refactors preserve prior kind ordinals, `NodeID` derivation rules, source ownership, baseline GFM parser profile, and fail-closed behavior; M104 appends `KindFootnoteDefinition` and M105 appends `KindMathExpression` without renumbering earlier kinds, while M106 adds no structural kind.

## Safety and authority boundaries

Prepared mutations fail closed on stale source, malformed/overlapping ranges, invalid target kinds, incomplete ownership, or ambiguous structure. Root/core code does not perform arbitrary filesystem traversal, network requests, command execution, or host authorization.

External paths, URLs, and relationship targets are data unless a caller outside core explicitly provides bounded authority. M100 implements this boundary by accepting only already-parsed caller-provided documents plus a build-only resolver whose successful target must already belong to that explicit set; it performs no filesystem/network discovery. M101 preserves that boundary: its resolver explicitly distinguishes ignored, resolved, and caller-declared missing relationships, is never retained, and cannot authorize filesystem/network discovery. M107 preserves the same explicit M100 graph boundary for syntax-independent knowledge metadata. M116 adds one deliberately separate adapter: the caller must supply the `fs.FS`, workspace root/entries, and finite limits; `workspacefs` uses only that read interface, performs no network/command/write operation, and returns an immutable finite document set to the existing M100/M101 logic. M117 defines the adapter's local relationship policy without widening that authority: only reviewed relative Markdown URI paths become filesystem candidates after one component-wise percent decode and source-relative normalization; query text is not part of lookup, fragments remain relationship metadata, encoded/root-escaping traversal and encoded separators fail closed, and absolute/scheme/protocol-relative/backslash/directory/extensionless forms remain outside filesystem resolution. Case and symlink semantics remain those of the caller's `fs.FS`.

Dependencies should remain minimal. Exact dependency versions belong in `go.mod` and `go.sum`; licensing belongs in `LICENSE` and `NOTICE`.

## Public module and release boundary

The Go module path is `github.com/zoster81/marksplice`. `go.mod` owns the minimum supported Go version and exact dependency graph; public release tags own module versions. Runtime code does not contain a second release-version authority. While the API remains under active development, public releases stay in the v0 series and may use explicit pre-release identifiers. Published Go module tags are immutable. Public versioning, release readiness, CI compatibility policy, and proxy/pkg.go.dev verification are owned by `docs/releasing.md` rather than by runtime packages.

M90 adds no Markdown/runtime behavior. It prepares public Go-module consumption through portable package examples, cross-platform public CI, dependency-update metadata, beta/release documentation, security-reporting guidance, and an external consumer-module compilation test. The separately licensed, hash-pinned GFM conformance corpus remains external to the repository and therefore remains a stricter maintainer gate rather than a vendored public-CI input.

M91 is a repository-layout refactor only. It moves the black-box public API tests from the module root to `internal/publictest`, groups root package filenames into `api*` and `builder*` families, and adds `docs/README.md` as the documentation index. It does not change the module path, package name, exported API, Markdown behavior, or ordinary consumer import path. Coverage gates after M91 must use cross-package instrumentation because the consumer-style tests now live in a separate package directory.

## Public capability and history references

The repository `README.md` is the single public entry point. `docs/getting-started.md`, `docs/guide.md`, `docs/recipes/`, and `examples/` form the progressive user-learning path; `docs/api-reference.md` is the exhaustive exported-callable reference for the root package plus the separate `workspacefs` adapter; and `docs/capabilities.md` is the authoritative concise current capability boundary. `docs/README.md` maps user, advanced/maintainer, and historical documentation without acting as a second home page. `docs/goldmark-capability-matrix.md` is the historical pre-M115 parser/source ownership transition record, `docs/releasing.md` records public module/version policy, and `docs/milestones/` retains the detailed M0–M121 contracts, design evolution, and historical verification evidence. M121 is locally complete; ordinary milestone freeze, push, and exact remote CI verification are required before M122 implementation begins.
